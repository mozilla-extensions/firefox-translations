version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest-browsers

    working_directory: ~/checkout

    steps:
      - checkout

      # Download and cache dependencies (update the cache keys to force a newer version)
      - restore_cache:
          keys:
            - dependencies-and-browsers-{{ checksum "extension/yarn.lock" }}-{{ checksum "extension/download-bergamot-translator-wasm-artifacts.sh" }}-20210224

      - run:
          name: Download and import Bergamot Translator build artifacts
          # Note: the import step runs again as part of yarn build, which ensures that the latest .appendix.js is used despite cached artifacts
          command: cd extension && yarn bergamot-translator:download-and-import

      - run:
          name: Install dependencies
          command: cd extension && yarn install --frozen-lockfile

      - run:
          name:  Install Firefox
          command: |
            cd extension
            yarn get-firefox:linux
            # Prevent any tests from using the default installed version of Firefox by accident
            sudo rm /usr/bin/firefox || true

      - save_cache:
          paths:
            - extension/downloaded-bergamot-translator-wasm-artifacts
            - extension/node_modules
            - extension/firefox-nightly-linux
          key: dependencies-and-browsers-{{ checksum "extension/yarn.lock" }}-{{ checksum "extension/download-bergamot-translator-wasm-artifacts.sh" }}-20210224

      - run:
          name: Print which version of Firefox is installed to the build log
          command: |
            cd extension
            echo "Version of Firefox installed:"
            ./firefox-nightly-linux/firefox-bin -v

      - run:
          name: Lint
          command: cd extension && yarn lint

      - run:
          name: Build extension
          command: cd extension && yarn build

      - store_artifacts:
          path: "extension/dist"
          destination: "extension/dist"

      - run:
          name: Functional tests
          command: cd extension && FIREFOX_BINARY=./firefox-nightly-linux/firefox-bin yarn just-test
